# UM4MA316_Students
Student repo (one branch per student) for the cours UM4MA316

```bash
git clone https://[token]@github.com/didiersmets/UM4MA316_Students.git
```
replacing token with its actual value given during 1st TP class.


TP0

2_hello.c

3_pascal.c uses stack allocated array
3_2_pascal.c uses heap allocated array (size is kept at 1000 * float max)


4_read_file.c uses stack allocated array
4_write_file.c uses stack allocated array
4_2_write_file.c uses heap allocated array (size is kept at 1000 * double max)
