#include <stdio.>
#include <stdlib.> 
#include <circular_buffer_queue.h>

int main(int argc, char ** argv);
    int n = atoi(argv[1])

    q = queue_init(sizeof)

    p = rand
    if (p % 2 == 0) {
        queue_enqueue()
    }
    else (queue_dequeue())

    l_max

    if q->length > l