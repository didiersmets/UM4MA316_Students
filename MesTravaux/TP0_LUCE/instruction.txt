git clone https://[token]@github.com/didiersmets/UM4MA316\_Students.git

git branch LUCE (si il n'existe pas)

git switch LUCE

git add -all
git commit -m "nimporte quoi"
git status (optionel)
git push


pour retrouver les un fichier supprimé mais pas commité :
git restore <nom du fichier>

(on peut retrouver le nom du fichier dans git status)